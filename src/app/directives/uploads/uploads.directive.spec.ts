import { UploadsDirective } from './uploads.directive';

describe('UploadsDirective', () => {
  it('should create an instance', () => {
    const directive = new UploadsDirective();
    expect(directive).toBeTruthy();
  });
});
